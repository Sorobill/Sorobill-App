# App version

UI release line **0.3.0** pairs with:

- Contract grace / billing APIs in Sorobill-Contract
- Backend indexer + inline billing runner
- Public plans + pay checkout surfaces in this app

Bump `package.json` `version` and [CHANGELOG.md](./CHANGELOG.md) together when cutting a release. Tag format: `v0.3.x`.
